WorkloadDefinition:
  Name: "sample-app"
  YAMLDocument: "sample-app-manifest.yaml"
  UserID: 1   # can be derived from config
---
WorkloadInstance:
  Name: "sample-app-0"
  ClusterInstanceID: 1   # can be defaulted or looked up by name
  WorkloadDefinitionID: 1   # can be looked up by name
  UserID: 1   # can be derived from config

