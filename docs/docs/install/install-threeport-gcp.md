# Install Threeport on GCP

TODO:

* In prerequisites add these setup commands:
    * Have `gcloud` CLI installed and configured
    * Run `gcloud auth application-default login` to set up credentials.
    * Enable Kubernetes Engine API in Google Cloud

    * ??? Install Pulumi GCP plugin: `pulumi plugin install resource gcp v8.41.1`


