// generated by 'threeport-codegen controller' - do not edit

package radiusworkload

import (
	"errors"
	"fmt"
	v0 "github.com/threeport/threeport/pkg/api/v0"
	client "github.com/threeport/threeport/pkg/client/v0"
	controller "github.com/threeport/threeport/pkg/controller/v0"
	notifications "github.com/threeport/threeport/pkg/notifications/v0"
	util "github.com/threeport/threeport/pkg/util/v0"
	"os"
	"os/signal"
	"syscall"
	"time"
)

// RadiusWorkloadInstanceReconciler reconciles system state when a RadiusWorkloadInstance
// is created, updated or deleted.
func RadiusWorkloadInstanceReconciler(r *controller.Reconciler) {
	r.ShutdownWait.Add(1)
	reconcilerLog := r.Log.WithValues("reconcilerName", r.Name)
	reconcilerLog.Info("reconciler started")
	shutdown := false

	// create a channel to receive OS signals
	osSignals := make(chan os.Signal, 1)
	lockReleased := make(chan bool, 1)

	// register the os signals channel to receive SIGINT and SIGTERM signals
	signal.Notify(osSignals, syscall.SIGINT, syscall.SIGTERM)

	for {
		// create a fresh log object per reconciliation loop so we don't
		// accumulate values across multiple loops
		log := r.Log.WithValues("reconcilerName", r.Name)

		if shutdown {
			break
		}

		// check for shutdown instruction
		select {
		case <-r.Shutdown:
			shutdown = true
		default:
			// pull message off queue
			msg := r.PullMessage()
			if msg == nil {
				continue
			}

			// consume message data to capture notification from API
			notif, err := notifications.ConsumeMessage(msg.Data)
			if err != nil {
				log.Error(
					err, "failed to consume message data from NATS",
					"msgData", string(msg.Data),
				)
				r.RequeueRaw(msg)
				log.V(1).Info("radius workload instance reconciliation requeued with identical payload and fixed delay")
				continue
			}

			// decode the object that was sent in the notification
			var radiusWorkloadInstance v0.RadiusWorkloadInstance
			if err := radiusWorkloadInstance.DecodeNotifObject(notif.Object); err != nil {
				log.Error(err, "failed to marshal object map from consumed notification message")
				r.RequeueRaw(msg)
				log.V(1).Info("radius workload instance reconciliation requeued with identical payload and fixed delay")
				continue
			}
			log = log.WithValues("radiusWorkloadInstanceID", radiusWorkloadInstance.ID)

			// back off the requeue delay as needed
			requeueDelay := controller.SetRequeueDelay(
				notif.CreationTime,
			)

			// check for lock on object
			locked, ok := r.CheckLock(&radiusWorkloadInstance)
			if locked || ok == false {
				r.Requeue(&radiusWorkloadInstance, requeueDelay, msg)
				log.V(1).Info("radius workload instance reconciliation requeued")
				continue
			}

			// set up handler to unlock and requeue on termination signal
			go func() {
				select {
				case <-osSignals:
					log.V(1).Info("received termination signal, performing unlock and requeue of radius workload instance")
					r.UnlockAndRequeue(&radiusWorkloadInstance, requeueDelay, lockReleased, msg)
				case <-lockReleased:
					log.V(1).Info("reached end of reconcile loop for radius workload instance, closing out signal handler")
				}
			}()

			// put a lock on the reconciliation of the created object
			if ok := r.Lock(&radiusWorkloadInstance); !ok {
				r.Requeue(&radiusWorkloadInstance, requeueDelay, msg)
				log.V(1).Info("radius workload instance reconciliation requeued")
				continue
			}

			// retrieve latest version of object
			latestRadiusWorkloadInstance, err := client.GetRadiusWorkloadInstanceByID(
				r.APIClient,
				r.APIServer,
				*radiusWorkloadInstance.ID,
			)
			// check if error is 404 - if object no longer exists, no need to requeue
			if errors.Is(err, client.ErrorObjectNotFound) {
				log.Info(fmt.Sprintf(
					"object with ID %d no longer exists - halting reconciliation",
					*radiusWorkloadInstance.ID,
				))
				r.ReleaseLock(&radiusWorkloadInstance, lockReleased, msg, true)
				continue
			}
			if err != nil {
				log.Error(err, "failed to get radius workload instance by ID from API")
				r.UnlockAndRequeue(&radiusWorkloadInstance, requeueDelay, lockReleased, msg)
				continue
			}
			radiusWorkloadInstance = *latestRadiusWorkloadInstance

			// determine which operation and act accordingly
			switch notif.Operation {
			case notifications.NotificationOperationCreated:
				if radiusWorkloadInstance.DeletionScheduled != nil {
					log.Info("radius workload instance scheduled for deletion - skipping create")
					break
				}
				customRequeueDelay, err := radiusWorkloadInstanceCreated(r, &radiusWorkloadInstance, &log)
				if err != nil {
					log.Error(err, "failed to reconcile created radius workload instance object")
					r.UnlockAndRequeue(
						&radiusWorkloadInstance,
						requeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
				if customRequeueDelay != 0 {
					log.Info("create requeued for future reconciliation")
					r.UnlockAndRequeue(
						&radiusWorkloadInstance,
						customRequeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
			case notifications.NotificationOperationUpdated:
				customRequeueDelay, err := radiusWorkloadInstanceUpdated(r, &radiusWorkloadInstance, &log)
				if err != nil {
					log.Error(err, "failed to reconcile updated radius workload instance object")
					r.UnlockAndRequeue(
						&radiusWorkloadInstance,
						requeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
				if customRequeueDelay != 0 {
					log.Info("update requeued for future reconciliation")
					r.UnlockAndRequeue(
						&radiusWorkloadInstance,
						customRequeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
			case notifications.NotificationOperationDeleted:
				customRequeueDelay, err := radiusWorkloadInstanceDeleted(r, &radiusWorkloadInstance, &log)
				if err != nil {
					log.Error(err, "failed to reconcile deleted radius workload instance object")
					r.UnlockAndRequeue(
						&radiusWorkloadInstance,
						requeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
				if customRequeueDelay != 0 {
					log.Info("deletion requeued for future reconciliation")
					r.UnlockAndRequeue(
						&radiusWorkloadInstance,
						customRequeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
				deletionTimestamp := util.TimePtr(time.Now().UTC())
				deletedRadiusWorkloadInstance := v0.RadiusWorkloadInstance{
					Common: v0.Common{ID: radiusWorkloadInstance.ID},
					Reconciliation: v0.Reconciliation{
						DeletionAcknowledged: deletionTimestamp,
						DeletionConfirmed:    deletionTimestamp,
						Reconciled:           util.BoolPtr(true),
					},
				}
				if err != nil {
					log.Error(err, "failed to update radius workload instance to mark as reconciled")
					r.UnlockAndRequeue(&radiusWorkloadInstance, requeueDelay, lockReleased, msg)
					continue
				}
				_, err = client.UpdateRadiusWorkloadInstance(
					r.APIClient,
					r.APIServer,
					&deletedRadiusWorkloadInstance,
				)
				if err != nil {
					log.Error(err, "failed to update radius workload instance to mark as deleted")
					r.UnlockAndRequeue(&radiusWorkloadInstance, requeueDelay, lockReleased, msg)
					continue
				}
				_, err = client.DeleteRadiusWorkloadInstance(
					r.APIClient,
					r.APIServer,
					*radiusWorkloadInstance.ID,
				)
				if err != nil {
					log.Error(err, "failed to delete radius workload instance")
					r.UnlockAndRequeue(&radiusWorkloadInstance, requeueDelay, lockReleased, msg)
					continue
				}
			default:
				log.Error(
					errors.New("unrecognized notifcation operation"),
					"notification included an invalid operation",
				)
				r.UnlockAndRequeue(
					&radiusWorkloadInstance,
					requeueDelay,
					lockReleased,
					msg,
				)
				continue

			}

			// set the object's Reconciled field to true if not deleted
			if notif.Operation != notifications.NotificationOperationDeleted {
				objectReconciled := true
				reconciledRadiusWorkloadInstance := v0.RadiusWorkloadInstance{
					Common:         v0.Common{ID: radiusWorkloadInstance.ID},
					Reconciliation: v0.Reconciliation{Reconciled: &objectReconciled},
				}
				updatedRadiusWorkloadInstance, err := client.UpdateRadiusWorkloadInstance(
					r.APIClient,
					r.APIServer,
					&reconciledRadiusWorkloadInstance,
				)
				if err != nil {
					log.Error(err, "failed to update radius workload instance to mark as reconciled")
					r.UnlockAndRequeue(&radiusWorkloadInstance, requeueDelay, lockReleased, msg)
					continue
				}
				log.V(1).Info(
					"radius workload instance marked as reconciled in API",
					"radius workload instanceName", updatedRadiusWorkloadInstance.Name,
				)
			}

			// release the lock on the reconciliation of the created object
			if ok := r.ReleaseLock(&radiusWorkloadInstance, lockReleased, msg, true); !ok {
				log.Error(errors.New("radius workload instance remains locked - will unlock when TTL expires"), "")
			} else {
				log.V(1).Info("radius workload instance unlocked")
			}

			log.Info(fmt.Sprintf(
				"radius workload instance successfully reconciled for %s operation",
				notif.Operation,
			))
		}
	}

	r.Sub.Unsubscribe()
	reconcilerLog.Info("reconciler shutting down")
	r.ShutdownWait.Done()
}
