# Requirements For Adding Aliases That Shorten The API Object Names

Example: Users could type `tptctl get kri` instead of `tptctl get kubernetes-runtime-instances`.

`ai/cli-aliases/tptctl-command-alias.md` contains the abbreviations for API objects to use as aliases on the appropriate commands.

The files that need to change can be located in `cmd/tptctl/cmd`.