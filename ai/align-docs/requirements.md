# Requirements for Updating Workload Names in Threeport Docs

## update 1
We need to update all instances of the word "Workload" only when it is talking about a Defined Instance Abstraction (This term is defined in `docs/docs/concepts/definitions-instances.md`). The new term should be "KubernetesWorkload".


## update 2
ALL instances of the term "WorkloadDefinition" ---> "KubernetesWorkloadDefinition"
ALL instances of the term "WorkloadInstance" ---> "KubernetesWorkloadInstance"

Note: All svg diagrams in the docs should be updated too if needed

All docs are in `docs`.


