// generated by 'threeport-sdk gen' but will not be regenerated - intended for modification

package cmd

import (
	"fmt"
	"os"
	"text/tabwriter"

	config_v0 "github.com/threeport/threeport/pkg/config/v0"
)

// outputGetv0ProfilesCmd produces the tabular output for the
// `get profiles` command.
func outputGetv0ProfilesCmd(
	profiles *[]config_v0.ProfileConfig,
) error {
	writer := tabwriter.NewWriter(os.Stdout, 4, 4, 4, ' ', 0)
	fmt.Fprintln(writer, "NAME\t AGE")
	for _, profile := range *profiles {
		fmt.Fprintln(
			writer,
			*profile.Profile.Name, "\t",
			*profile.Profile.Age,
		)
	}
	writer.Flush()

	return nil
}

// outputGetv0TiersCmd produces the tabular output for the
// `get tiers` command.
func outputGetv0TiersCmd(
	tiers *[]config_v0.TierConfig,
) error {
	writer := tabwriter.NewWriter(os.Stdout, 4, 4, 4, ' ', 0)
	fmt.Fprintln(writer, "NAME\t CRITICALITY\t AGE")
	for _, tier := range *tiers {
		fmt.Fprintln(
			writer,
			*tier.Tier.Name, "\t",
			*tier.Tier.Criticality, "\t",
			*tier.Tier.Age,
		)
	}
	writer.Flush()

	return nil
}
