// generated by 'threeport-sdk gen' but will not be regenerated - intended for modification

package cmd

import (
	"fmt"
	"net/http"

	api_v0 "github.com/threeport/threeport/pkg/api/v0"
	config_v0 "github.com/threeport/threeport/pkg/config/v0"
)

// outputDescribev0ModuleApiCmd produces the plain description
// output for the 'describe module-api' command
func outputDescribev0ModuleApiCmd(
	moduleApi *api_v0.ModuleApi,
	moduleApiConfig *config_v0.ModuleApiConfig,
	apiClient *http.Client,
	apiEndpoint string,
) error {
	// output describe details
	fmt.Printf(
		"* ModuleApi Name: %s\n",
		*moduleApiConfig.ModuleApi.Name,
	)
	fmt.Printf(
		"* Created: %s\n",
		*moduleApi.CreatedAt,
	)
	fmt.Printf(
		"* Last Modified: %s\n",
		*moduleApi.UpdatedAt,
	)

	return nil
}

// outputDescribev0ModuleApiRouteCmd produces the plain description
// output for the 'describe module-api-route' command
func outputDescribev0ModuleApiRouteCmd(
	moduleApiRoute *api_v0.ModuleApiRoute,
	moduleApiRouteConfig *config_v0.ModuleApiRouteConfig,
	apiClient *http.Client,
	apiEndpoint string,
) error {
	// output describe details
	fmt.Printf(
		"* ModuleApiRoute Path: %s\n",
		*moduleApiRouteConfig.ModuleApiRoute.Path,
	)
	fmt.Printf(
		"* Created: %s\n",
		*moduleApiRoute.CreatedAt,
	)
	fmt.Printf(
		"* Last Modified: %s\n",
		*moduleApiRoute.UpdatedAt,
	)

	return nil
}

// outputDescribev0ModuleControllerCmd produces the plain description
// output for the 'describe module-controller' command
func outputDescribev0ModuleControllerCmd(
	moduleController *api_v0.ModuleController,
	moduleControllerConfig *config_v0.ModuleControllerConfig,
	apiClient *http.Client,
	apiEndpoint string,
) error {
	// output describe details
	fmt.Printf(
		"* ModuleController Name: %s\n",
		*moduleControllerConfig.ModuleController.Name,
	)
	fmt.Printf(
		"* Created: %s\n",
		*moduleController.CreatedAt,
	)
	fmt.Printf(
		"* Last Modified: %s\n",
		*moduleController.UpdatedAt,
	)

	return nil
}

// outputDescribev0ModuleObjectCmd produces the plain description
// output for the 'describe module-object' command
func outputDescribev0ModuleObjectCmd(
	moduleObject *api_v0.ModuleObject,
	moduleObjectConfig *config_v0.ModuleObjectConfig,
	apiClient *http.Client,
	apiEndpoint string,
) error {
	// output describe details
	fmt.Printf(
		"* ModuleObject Name: %s\n",
		*moduleObjectConfig.ModuleObject.Name,
	)
	fmt.Printf(
		"* Created: %s\n",
		*moduleObject.CreatedAt,
	)
	fmt.Printf(
		"* Last Modified: %s\n",
		*moduleObject.UpdatedAt,
	)

	return nil
}
