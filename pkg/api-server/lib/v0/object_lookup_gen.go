// boilerplate generated by 'threeport-sdk gen' - do not edit

package v0

import (
	"errors"
	"fmt"
	v0 "github.com/threeport/threeport/pkg/api/v0"
	gorm "gorm.io/gorm"
)

// ErrUnknownCoreType signals that the given object type is not
// owned by core. Callers should dispatch to the owning module.
var ErrUnknownCoreType = errors.New("object type not owned by core")

// GetCoreObjectNamesByIDs returns id->name for each id of the given
// core object type, or ErrUnknownCoreType if the type isn't a core type.
// includeDeleted=true bypasses the soft-delete filter to include removed rows.
func GetCoreObjectNamesByIDs(db *gorm.DB, objectType string, ids []uint, includeDeleted bool) (map[uint]string, error) {
	if len(ids) == 0 {
		return map[uint]string{}, nil
	}
	if includeDeleted {
		db = db.Unscoped()
	}
	out := make(map[uint]string, len(ids))
	switch objectType {
	case "threeport.io/v0.AwsEksKubernetesRuntimeDefinition":
		var rows []v0.AwsEksKubernetesRuntimeDefinition
		if err := db.Model(&v0.AwsEksKubernetesRuntimeDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up AwsEksKubernetesRuntimeDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.AwsEksKubernetesRuntimeInstance":
		var rows []v0.AwsEksKubernetesRuntimeInstance
		if err := db.Model(&v0.AwsEksKubernetesRuntimeInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up AwsEksKubernetesRuntimeInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.AwsProvider":
		var rows []v0.AwsProvider
		if err := db.Model(&v0.AwsProvider{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up AwsProvider names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.ControlPlaneDefinition":
		var rows []v0.ControlPlaneDefinition
		if err := db.Model(&v0.ControlPlaneDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ControlPlaneDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.ControlPlaneInstance":
		var rows []v0.ControlPlaneInstance
		if err := db.Model(&v0.ControlPlaneInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ControlPlaneInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.DomainNameDefinition":
		var rows []v0.DomainNameDefinition
		if err := db.Model(&v0.DomainNameDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up DomainNameDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.DomainNameInstance":
		var rows []v0.DomainNameInstance
		if err := db.Model(&v0.DomainNameInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up DomainNameInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.GatewayDefinition":
		var rows []v0.GatewayDefinition
		if err := db.Model(&v0.GatewayDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GatewayDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.GatewayInstance":
		var rows []v0.GatewayInstance
		if err := db.Model(&v0.GatewayInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GatewayInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.GcpGkeKubernetesRuntimeDefinition":
		var rows []v0.GcpGkeKubernetesRuntimeDefinition
		if err := db.Model(&v0.GcpGkeKubernetesRuntimeDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GcpGkeKubernetesRuntimeDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.GcpGkeKubernetesRuntimeInstance":
		var rows []v0.GcpGkeKubernetesRuntimeInstance
		if err := db.Model(&v0.GcpGkeKubernetesRuntimeInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GcpGkeKubernetesRuntimeInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.GcpProvider":
		var rows []v0.GcpProvider
		if err := db.Model(&v0.GcpProvider{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GcpProvider names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.HelmWorkloadDefinition":
		var rows []v0.HelmWorkloadDefinition
		if err := db.Model(&v0.HelmWorkloadDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up HelmWorkloadDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.HelmWorkloadInstance":
		var rows []v0.HelmWorkloadInstance
		if err := db.Model(&v0.HelmWorkloadInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up HelmWorkloadInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.KubernetesRuntimeDefinition":
		var rows []v0.KubernetesRuntimeDefinition
		if err := db.Model(&v0.KubernetesRuntimeDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesRuntimeDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.KubernetesRuntimeInstance":
		var rows []v0.KubernetesRuntimeInstance
		if err := db.Model(&v0.KubernetesRuntimeInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesRuntimeInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.KubernetesWorkloadDefinition":
		var rows []v0.KubernetesWorkloadDefinition
		if err := db.Model(&v0.KubernetesWorkloadDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesWorkloadDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.KubernetesWorkloadInstance":
		var rows []v0.KubernetesWorkloadInstance
		if err := db.Model(&v0.KubernetesWorkloadInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesWorkloadInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.LogBackend":
		var rows []v0.LogBackend
		if err := db.Model(&v0.LogBackend{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LogBackend names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.LogStorageDefinition":
		var rows []v0.LogStorageDefinition
		if err := db.Model(&v0.LogStorageDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LogStorageDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.LogStorageInstance":
		var rows []v0.LogStorageInstance
		if err := db.Model(&v0.LogStorageInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LogStorageInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.LoggingDefinition":
		var rows []v0.LoggingDefinition
		if err := db.Model(&v0.LoggingDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LoggingDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.LoggingInstance":
		var rows []v0.LoggingInstance
		if err := db.Model(&v0.LoggingInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LoggingInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.MachineRuntimeDefinition":
		var rows []v0.MachineRuntimeDefinition
		if err := db.Model(&v0.MachineRuntimeDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineRuntimeDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.MachineRuntimeInstance":
		var rows []v0.MachineRuntimeInstance
		if err := db.Model(&v0.MachineRuntimeInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineRuntimeInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.MachineWorkloadDefinition":
		var rows []v0.MachineWorkloadDefinition
		if err := db.Model(&v0.MachineWorkloadDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineWorkloadDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.MachineWorkloadInstance":
		var rows []v0.MachineWorkloadInstance
		if err := db.Model(&v0.MachineWorkloadInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineWorkloadInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.MetricsDefinition":
		var rows []v0.MetricsDefinition
		if err := db.Model(&v0.MetricsDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MetricsDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.MetricsInstance":
		var rows []v0.MetricsInstance
		if err := db.Model(&v0.MetricsInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MetricsInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.ModuleApi":
		var rows []v0.ModuleApi
		if err := db.Model(&v0.ModuleApi{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ModuleApi names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.ModuleController":
		var rows []v0.ModuleController
		if err := db.Model(&v0.ModuleController{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ModuleController names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.ModuleObject":
		var rows []v0.ModuleObject
		if err := db.Model(&v0.ModuleObject{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ModuleObject names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.ObservabilityDashboardDefinition":
		var rows []v0.ObservabilityDashboardDefinition
		if err := db.Model(&v0.ObservabilityDashboardDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityDashboardDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.ObservabilityDashboardInstance":
		var rows []v0.ObservabilityDashboardInstance
		if err := db.Model(&v0.ObservabilityDashboardInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityDashboardInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.ObservabilityStackDefinition":
		var rows []v0.ObservabilityStackDefinition
		if err := db.Model(&v0.ObservabilityStackDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityStackDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.ObservabilityStackInstance":
		var rows []v0.ObservabilityStackInstance
		if err := db.Model(&v0.ObservabilityStackInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityStackInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.OciOkeKubernetesRuntimeDefinition":
		var rows []v0.OciOkeKubernetesRuntimeDefinition
		if err := db.Model(&v0.OciOkeKubernetesRuntimeDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up OciOkeKubernetesRuntimeDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.OciOkeKubernetesRuntimeInstance":
		var rows []v0.OciOkeKubernetesRuntimeInstance
		if err := db.Model(&v0.OciOkeKubernetesRuntimeInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up OciOkeKubernetesRuntimeInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.OciProvider":
		var rows []v0.OciProvider
		if err := db.Model(&v0.OciProvider{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up OciProvider names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.Profile":
		var rows []v0.Profile
		if err := db.Model(&v0.Profile{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up Profile names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.SecretDefinition":
		var rows []v0.SecretDefinition
		if err := db.Model(&v0.SecretDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up SecretDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.SecretInstance":
		var rows []v0.SecretInstance
		if err := db.Model(&v0.SecretInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up SecretInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.TerraformDefinition":
		var rows []v0.TerraformDefinition
		if err := db.Model(&v0.TerraformDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up TerraformDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.TerraformInstance":
		var rows []v0.TerraformInstance
		if err := db.Model(&v0.TerraformInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up TerraformInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "threeport.io/v0.Tier":
		var rows []v0.Tier
		if err := db.Model(&v0.Tier{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up Tier names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	default:
		return nil, ErrUnknownCoreType
	}
	return out, nil
}

// GetCoreObjectIDsByName returns every ID with the given name for
// the given core object type, or ErrUnknownCoreType if the type
// isn't a core type. Empty result is returned as an empty slice
// with no error.
func GetCoreObjectIDsByName(db *gorm.DB, objectType string, name string) ([]uint, error) {
	switch objectType {
	case "threeport.io/v0.AwsEksKubernetesRuntimeDefinition":
		var rows []v0.AwsEksKubernetesRuntimeDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up AwsEksKubernetesRuntimeDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.AwsEksKubernetesRuntimeInstance":
		var rows []v0.AwsEksKubernetesRuntimeInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up AwsEksKubernetesRuntimeInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.AwsProvider":
		var rows []v0.AwsProvider
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up AwsProvider by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.ControlPlaneDefinition":
		var rows []v0.ControlPlaneDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ControlPlaneDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.ControlPlaneInstance":
		var rows []v0.ControlPlaneInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ControlPlaneInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.DomainNameDefinition":
		var rows []v0.DomainNameDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up DomainNameDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.DomainNameInstance":
		var rows []v0.DomainNameInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up DomainNameInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.GatewayDefinition":
		var rows []v0.GatewayDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GatewayDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.GatewayInstance":
		var rows []v0.GatewayInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GatewayInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.GcpGkeKubernetesRuntimeDefinition":
		var rows []v0.GcpGkeKubernetesRuntimeDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GcpGkeKubernetesRuntimeDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.GcpGkeKubernetesRuntimeInstance":
		var rows []v0.GcpGkeKubernetesRuntimeInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GcpGkeKubernetesRuntimeInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.GcpProvider":
		var rows []v0.GcpProvider
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GcpProvider by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.HelmWorkloadDefinition":
		var rows []v0.HelmWorkloadDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up HelmWorkloadDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.HelmWorkloadInstance":
		var rows []v0.HelmWorkloadInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up HelmWorkloadInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.KubernetesRuntimeDefinition":
		var rows []v0.KubernetesRuntimeDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesRuntimeDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.KubernetesRuntimeInstance":
		var rows []v0.KubernetesRuntimeInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesRuntimeInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.KubernetesWorkloadDefinition":
		var rows []v0.KubernetesWorkloadDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesWorkloadDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.KubernetesWorkloadInstance":
		var rows []v0.KubernetesWorkloadInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesWorkloadInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.LogBackend":
		var rows []v0.LogBackend
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LogBackend by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.LogStorageDefinition":
		var rows []v0.LogStorageDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LogStorageDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.LogStorageInstance":
		var rows []v0.LogStorageInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LogStorageInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.LoggingDefinition":
		var rows []v0.LoggingDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LoggingDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.LoggingInstance":
		var rows []v0.LoggingInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LoggingInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.MachineRuntimeDefinition":
		var rows []v0.MachineRuntimeDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineRuntimeDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.MachineRuntimeInstance":
		var rows []v0.MachineRuntimeInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineRuntimeInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.MachineWorkloadDefinition":
		var rows []v0.MachineWorkloadDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineWorkloadDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.MachineWorkloadInstance":
		var rows []v0.MachineWorkloadInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineWorkloadInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.MetricsDefinition":
		var rows []v0.MetricsDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MetricsDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.MetricsInstance":
		var rows []v0.MetricsInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MetricsInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.ModuleApi":
		var rows []v0.ModuleApi
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ModuleApi by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.ModuleController":
		var rows []v0.ModuleController
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ModuleController by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.ModuleObject":
		var rows []v0.ModuleObject
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ModuleObject by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.ObservabilityDashboardDefinition":
		var rows []v0.ObservabilityDashboardDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityDashboardDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.ObservabilityDashboardInstance":
		var rows []v0.ObservabilityDashboardInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityDashboardInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.ObservabilityStackDefinition":
		var rows []v0.ObservabilityStackDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityStackDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.ObservabilityStackInstance":
		var rows []v0.ObservabilityStackInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityStackInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.OciOkeKubernetesRuntimeDefinition":
		var rows []v0.OciOkeKubernetesRuntimeDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up OciOkeKubernetesRuntimeDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.OciOkeKubernetesRuntimeInstance":
		var rows []v0.OciOkeKubernetesRuntimeInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up OciOkeKubernetesRuntimeInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.OciProvider":
		var rows []v0.OciProvider
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up OciProvider by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.Profile":
		var rows []v0.Profile
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up Profile by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.SecretDefinition":
		var rows []v0.SecretDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up SecretDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.SecretInstance":
		var rows []v0.SecretInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up SecretInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.TerraformDefinition":
		var rows []v0.TerraformDefinition
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up TerraformDefinition by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.TerraformInstance":
		var rows []v0.TerraformInstance
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up TerraformInstance by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	case "threeport.io/v0.Tier":
		var rows []v0.Tier
		if err := db.Select("id").Where("name = ?", name).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up Tier by name: %w", err)
		}
		ids := make([]uint, 0, len(rows))
		for _, r := range rows {
			if r.ID != nil {
				ids = append(ids, *r.ID)
			}
		}
		return ids, nil

	default:
		return nil, ErrUnknownCoreType
	}
}
