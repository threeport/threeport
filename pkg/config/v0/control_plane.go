// generated by 'threeport-sdk gen' but will not be regenerated - intended for modification

package v0

import (
	"fmt"
	"net/http"

	api_v0 "github.com/threeport/threeport/pkg/api/v0"
	util "github.com/threeport/threeport/pkg/util/v0"
)

// ControlPlaneConfig is a container for a ControlPlane which is a config abstraction for
// the ControlPlaneDefinition and ControlPlaneInstance API objects.
// This abstraction allows users to manage definitions and instances together with single operations
// rather than separate operations for each API object.
type ControlPlaneConfig struct {
	ControlPlane ControlPlaneValues `yaml:"ControlPlane"`
}

// ControlPlaneValues contains all the attributes needed to manage the
// ControlPlaneDefinition and ControlPlaneInstance API objects
// together with a single operation.
type ControlPlaneValues struct {
	Name                      *string                          `json:"Name,omitempty" yaml:"Name,omitempty"`
	Namespace                 *string                          `json:"Namespace,omitempty" yaml:"Namespace,omitempty"`
	AuthEnabled               *bool                            `json:"AuthEnabled,omitempty" yaml:"AuthEnabled,omitempty"`
	OnboardParent             *bool                            `json:"OnboardParent,omitempty" yaml:"OnboardParent,omitempty"`
	KubernetesRuntimeInstance *KubernetesRuntimeInstanceValues `json:"KubernetesRuntimeInstance,omitempty" yaml:"KubernetesRuntimeInstance,omitempty"`
	CustomComponentInfo       []*api_v0.ControlPlaneComponent  `json:"CustomComponentInfo,omitempty" yaml:"CustomComponentInfo,omitempty"`
	Genesis                   *bool                            `json:"Genesis,omitempty" yaml:"Genesis,omitempty"`
	Age                       *string                          `json:"Age,omitempty" yaml:"Age,omitempty"`
}

// Get gets a control plane definition and instance from the Threeport API.
func (c *ControlPlaneConfig) Get(
	apiClient *http.Client,
	apiEndpoint string,
) (*[]ControlPlaneConfig, error) {
	// get operations
	operations, controlPlaneDefinitions, controlPlaneInstances := c.GetOperations(
		apiClient,
		apiEndpoint,
	)

	// execute get operations
	if err := operations.Get(); err != nil {
		return nil, fmt.Errorf(
			"failed to execute get operations for control plane defined instances: %w",
			err,
		)
	}

	// assemble the defined instances
	controlPlaneConfigs := mapToControlPlaneDefinedInstances(controlPlaneDefinitions, controlPlaneInstances)

	return controlPlaneConfigs, nil
}

// Create creates a control plane definition and instance in the Threeport API.
func (c *ControlPlaneConfig) Create(
	apiClient *http.Client,
	apiEndpoint string,
) (*[]ControlPlaneConfig, error) {
	// get operations
	operations, controlPlaneDefinitions, controlPlaneInstances := c.GetOperations(
		apiClient,
		apiEndpoint,
	)

	// execute create operations
	if err := operations.Create(); err != nil {
		return nil, fmt.Errorf(
			"failed to execute create operations for control plane defined instance with name %s: %w",
			*c.ControlPlane.Name,
			err,
		)
	}

	// assemble the defined instances
	controlPlaneConfigs := mapToControlPlaneDefinedInstances(controlPlaneDefinitions, controlPlaneInstances)

	return controlPlaneConfigs, nil
}

// Replace replaces a control plane definition and instance in the Threeport API.
func (c *ControlPlaneConfig) Replace(
	apiClient *http.Client,
	apiEndpoint string,
	name string,
) (*[]ControlPlaneConfig, error) {
	// get operations
	operations, controlPlaneDefinitions, controlPlaneInstances := c.GetOperations(
		apiClient,
		apiEndpoint,
	)

	// execute replace operations
	if err := operations.Replace(name); err != nil {
		return nil, fmt.Errorf(
			"failed to execute replace operations for control plane defined instance with name %s: %w",
			name,
			err,
		)
	}

	// assemble the defined instances
	controlPlaneConfigs := mapToControlPlaneDefinedInstances(controlPlaneDefinitions, controlPlaneInstances)

	return controlPlaneConfigs, nil
}

// Delete deletes a control plane definition and instance from the Threeport API.
func (c *ControlPlaneConfig) Delete(
	apiClient *http.Client,
	apiEndpoint string,
) (*[]ControlPlaneConfig, error) {
	// get operations
	operations, _, _ := c.GetOperations(
		apiClient,
		apiEndpoint,
	)

	// execute delete operations
	if err := operations.Delete(); err != nil {
		return nil, fmt.Errorf(
			"failed to execute delete operations for control plane defined instance with name %s: %w",
			*c.ControlPlane.Name,
			err,
		)
	}

	return nil, nil
}

// GetOperations returns a slice of operations used to get, create, replace or delete
// a control plane defined instance.
func (c *ControlPlaneConfig) GetOperations(
	apiClient *http.Client,
	apiEndpoint string,
) (*util.Operations, *[]ControlPlaneDefinitionConfig, *[]ControlPlaneInstanceConfig) {
	var err error
	var operatedControlPlaneDefinitions []ControlPlaneDefinitionConfig
	var operatedControlPlaneInstances []ControlPlaneInstanceConfig

	operations := util.Operations{}

	// add control plane definition operation
	controlPlaneDefinitionConfig := ControlPlaneDefinitionConfig{
		ControlPlaneDefinition: ControlPlaneDefinitionValues{
			Name:          c.ControlPlane.Name,
			AuthEnabled:   c.ControlPlane.AuthEnabled,
			OnboardParent: c.ControlPlane.OnboardParent,
		},
	}
	operations.AppendOperation(util.Operation{
		Create: func() error {
			controlPlaneDefinition, err := controlPlaneDefinitionConfig.Create(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to create control plane definition with name %s: %w", *c.ControlPlane.Name, err)
			}
			operatedControlPlaneDefinitions = append(operatedControlPlaneDefinitions, *controlPlaneDefinition)
			return nil
		},
		Delete: func() error {
			_, err = controlPlaneDefinitionConfig.Delete(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to delete control plane definition with name %s: %w", *c.ControlPlane.Name, err)
			}
			return nil
		},
		Get: func() error {
			controlPlaneDefinitions, err := controlPlaneDefinitionConfig.Get(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to get control plane definitions: %w", err)
			}
			operatedControlPlaneDefinitions = append(operatedControlPlaneDefinitions, *controlPlaneDefinitions...)
			return nil
		},
		Name: "control plane definition",
		Replace: func(name string) error {
			controlPlaneDefinition, err := controlPlaneDefinitionConfig.Replace(apiClient, apiEndpoint, name)
			if err != nil {
				return fmt.Errorf("failed to replace control plane definition with name %s: %w", name, err)
			}
			operatedControlPlaneDefinitions = append(operatedControlPlaneDefinitions, *controlPlaneDefinition)
			return nil
		},
	})

	// add control plane instance operation
	controlPlaneInstanceConfig := ControlPlaneInstanceConfig{
		ControlPlaneInstance: ControlPlaneInstanceValues{
			Name:                      c.ControlPlane.Name,
			Namespace:                 c.ControlPlane.Namespace,
			KubernetesRuntimeInstance: c.ControlPlane.KubernetesRuntimeInstance,
			CustomComponentInfo:       c.ControlPlane.CustomComponentInfo,
			ControlPlaneDefinition: &ControlPlaneDefinitionValues{
				Name: c.ControlPlane.Name,
			},
		},
	}
	operations.AppendOperation(util.Operation{
		Create: func() error {
			controlPlaneInstance, err := controlPlaneInstanceConfig.Create(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to create control plane instance with name %s: %w", *c.ControlPlane.Name, err)
			}
			operatedControlPlaneInstances = append(operatedControlPlaneInstances, *controlPlaneInstance)
			return nil
		},
		Delete: func() error {
			_, err = controlPlaneInstanceConfig.Delete(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to delete control plane instance with name %s: %w", *c.ControlPlane.Name, err)
			}
			return nil
		},
		Get: func() error {
			controlPlaneInstance, err := controlPlaneInstanceConfig.Get(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to get control plane instance with name %s: %w", *c.ControlPlane.Name, err)
			}
			operatedControlPlaneInstances = append(operatedControlPlaneInstances, *controlPlaneInstance...)
			return nil
		},
		Name: "control plane instance",
		Replace: func(name string) error {
			controlPlaneInstance, err := controlPlaneInstanceConfig.Replace(apiClient, apiEndpoint, name)
			if err != nil {
				return fmt.Errorf("failed to replace control plane definition with name %s: %w", name, err)
			}
			operatedControlPlaneInstances = append(operatedControlPlaneInstances, *controlPlaneInstance)
			return nil
		},
	})

	return &operations, &operatedControlPlaneDefinitions, &operatedControlPlaneInstances
}

// mapToControlPlaneDefinedInstances maps a slice of control plane definition and instance configs
// to a slice of control plane config objects
func mapToControlPlaneDefinedInstances(
	controlPlaneDefinitions *[]ControlPlaneDefinitionConfig,
	controlPlaneInstances *[]ControlPlaneInstanceConfig,
) *[]ControlPlaneConfig {
	var controlPlaneConfigs []ControlPlaneConfig
	for _, inst := range *controlPlaneInstances {
		for _, def := range *controlPlaneDefinitions {
			instName := *inst.ControlPlaneInstance.Name
			defName := *def.ControlPlaneDefinition.Name
			// a defined instance must have matching names for definition and instance
			// and the definition must be associated with the instance
			if instName == defName && *inst.ControlPlaneInstance.ControlPlaneDefinition.Name == *def.ControlPlaneDefinition.Name {
				controlPlaneConfig := ControlPlaneConfig{
					ControlPlane: ControlPlaneValues{
						Name:                      inst.ControlPlaneInstance.Name,
						Namespace:                 inst.ControlPlaneInstance.Namespace,
						AuthEnabled:               def.ControlPlaneDefinition.AuthEnabled,
						OnboardParent:             def.ControlPlaneDefinition.OnboardParent,
						KubernetesRuntimeInstance: inst.ControlPlaneInstance.KubernetesRuntimeInstance,
						CustomComponentInfo:       inst.ControlPlaneInstance.CustomComponentInfo,
						Genesis:                   inst.ControlPlaneInstance.Genesis,
						Age:                       inst.ControlPlaneInstance.Age,
					},
				}
				controlPlaneConfigs = append(controlPlaneConfigs, controlPlaneConfig)
				// an instance can only have one matching definition for a defined instance
				// we can break out of the loop after finding the first matching definition
				break
			}
		}
	}

	return &controlPlaneConfigs
}
