// generated by 'threeport-sdk gen' but will not be regenerated - intended for modification

package v0

import (
	"fmt"
	"net/http"

	api_v0 "github.com/threeport/threeport/pkg/api/v0"
	util "github.com/threeport/threeport/pkg/util/v0"
)

// ControlPlaneConfig is a container for a ControlPlane which is a config abstraction for
// the ControlPlaneDefinition and ControlPlaneInstance API objects.
// This abstraction allows users to manage definitions and instances together with single operations
// rather than separate operations for each API object.
type ControlPlaneConfig struct {
	ControlPlane ControlPlaneValues `yaml:"ControlPlane"`
}

// ControlPlaneValues contains all the attributes needed to manage the
// ControlPlaneDefinition and ControlPlaneInstance API objects
// together with a single operation.
type ControlPlaneValues struct {
	Name                      *string                          `yaml:"Name"`
	Namespace                 *string                          `yaml:"Namespace"`
	AuthEnabled               *bool                            `yaml:"AuthEnabled"`
	OnboardParent             *bool                            `yaml:"OnboardParent"`
	KubernetesRuntimeInstance *KubernetesRuntimeInstanceValues `yaml:"KubernetesRuntimeInstance"`
	CustomComponentInfo       []*api_v0.ControlPlaneComponent  `yaml:"CustomComponentInfo"`
	Age                       *string                          `yaml:"Age"`
}

// Get gets a control plane definition and instance from the Threeport API.
func (c *ControlPlaneValues) Get(
	apiClient *http.Client,
	apiEndpoint string,
) (*ControlPlaneDefinitionConfig, *ControlPlaneInstanceConfig, error) {
	// get operations
	operations, controlPlaneDefinition, controlPlaneInstance := c.GetOperations(
		apiClient,
		apiEndpoint,
	)

	// execute get operations
	if err := operations.Get(); err != nil {
		return nil, nil, fmt.Errorf(
			"failed to execute get operations for control plane defined instance with name %s: %w",
			*c.Name,
			err,
		)
	}

	return controlPlaneDefinition, controlPlaneInstance, nil
}

// Create creates a control plane definition and instance in the Threeport API.
func (c *ControlPlaneValues) Create(
	apiClient *http.Client,
	apiEndpoint string,
) (*ControlPlaneDefinitionConfig, *ControlPlaneInstanceConfig, error) {
	// get operations
	operations, controlPlaneDefinition, controlPlaneInstance := c.GetOperations(
		apiClient,
		apiEndpoint,
	)

	// execute create operations
	if err := operations.Create(); err != nil {
		return nil, nil, fmt.Errorf(
			"failed to execute create operations for control plane defined instance with name %s: %w",
			*c.Name,
			err,
		)
	}

	return controlPlaneDefinition, controlPlaneInstance, nil
}

// Replace replaces a control plane definition and instance in the Threeport API.
func (c *ControlPlaneValues) Replace(
	apiClient *http.Client,
	apiEndpoint string,
	name string,
) (*ControlPlaneDefinitionConfig, *ControlPlaneInstanceConfig, error) {
	// get operations
	operations, controlPlaneDefinition, controlPlaneInstance := c.GetOperations(
		apiClient,
		apiEndpoint,
	)

	// execute replace operations
	if err := operations.Replace(name); err != nil {
		return nil, nil, fmt.Errorf(
			"failed to execute replace operations for control plane defined instance with name %s: %w",
			name,
			err,
		)
	}

	return controlPlaneDefinition, controlPlaneInstance, nil
}

// Delete deletes a control plane definition and instance from the Threeport API.
func (c *ControlPlaneValues) Delete(
	apiClient *http.Client,
	apiEndpoint string,
) (*ControlPlaneDefinitionConfig, *ControlPlaneInstanceConfig, error) {
	// get operations
	operations, _, _ := c.GetOperations(
		apiClient,
		apiEndpoint,
	)

	// execute delete operations
	if err := operations.Delete(); err != nil {
		return nil, nil, fmt.Errorf(
			"failed to execute delete operations for control plane defined instance with name %s: %w",
			*c.Name,
			err,
		)
	}

	return nil, nil, nil
}

// GetOperations returns a slice of operations used to get, create, replace or delete
// a control plane defined instance.
func (c *ControlPlaneValues) GetOperations(
	apiClient *http.Client,
	apiEndpoint string,
) (*util.Operations, *ControlPlaneDefinitionConfig, *ControlPlaneInstanceConfig) {
	var err error
	var operatedControlPlaneDefinition ControlPlaneDefinitionConfig
	var operatedControlPlaneInstance ControlPlaneInstanceConfig

	operations := util.Operations{}

	// add control plane definition operation
	controlPlaneDefinitionValues := ControlPlaneDefinitionValues{
		Name:          c.Name,
		AuthEnabled:   c.AuthEnabled,
		OnboardParent: c.OnboardParent,
	}
	operations.AppendOperation(util.Operation{
		Create: func() error {
			controlPlaneDefinition, err := controlPlaneDefinitionValues.Create(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to create control plane definition with name %s: %w", *c.Name, err)
			}
			operatedControlPlaneDefinition = *controlPlaneDefinition
			return nil
		},
		Delete: func() error {
			_, err = controlPlaneDefinitionValues.Delete(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to delete control plane definition with name %s: %w", *c.Name, err)
			}
			return nil
		},
		Get: func() error {
			controlPlaneDefinition, err := controlPlaneDefinitionValues.Get(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to get control plane definition with name %s: %w", *c.Name, err)
			}
			if len(*controlPlaneDefinition) == 0 {
				return fmt.Errorf("failed to find control plane definition with name %s: %w", *c.Name, err)
			}
			if len(*controlPlaneDefinition) > 1 {
				return fmt.Errorf("multiple control plane definitions found with name %s: %w", *c.Name, err)
			}
			operatedControlPlaneDefinition = (*controlPlaneDefinition)[0]
			return nil
		},
		Name: "control plane definition",
		Replace: func(name string) error {
			controlPlaneDefinition, err := controlPlaneDefinitionValues.Replace(apiClient, apiEndpoint, name)
			if err != nil {
				return fmt.Errorf("failed to replace control plane definition with name %s: %w", name, err)
			}
			operatedControlPlaneDefinition = *controlPlaneDefinition
			return nil
		},
	})

	// add control plane instance operation
	controlPlaneInstanceValues := ControlPlaneInstanceValues{
		Name:                      c.Name,
		Namespace:                 c.Namespace,
		KubernetesRuntimeInstance: c.KubernetesRuntimeInstance,
		CustomComponentInfo:       c.CustomComponentInfo,
		ControlPlaneDefinition: &ControlPlaneDefinitionValues{
			Name: c.Name,
		},
	}
	operations.AppendOperation(util.Operation{
		Create: func() error {
			controlPlaneInstance, err := controlPlaneInstanceValues.Create(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to create control plane instance with name %s: %w", *c.Name, err)
			}
			operatedControlPlaneInstance = *controlPlaneInstance
			return nil
		},
		Delete: func() error {
			_, err = controlPlaneInstanceValues.Delete(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to delete control plane instance with name %s: %w", *c.Name, err)
			}
			return nil
		},
		Get: func() error {
			controlPlaneInstance, err := controlPlaneInstanceValues.Get(apiClient, apiEndpoint)
			if err != nil {
				return fmt.Errorf("failed to get control plane instance with name %s: %w", *c.Name, err)
			}
			if len(*controlPlaneInstance) == 0 {
				return fmt.Errorf("failed to find control plane instance with name %s: %w", *c.Name, err)
			}
			if len(*controlPlaneInstance) > 1 {
				return fmt.Errorf("multiple control plane instances found with name %s: %w", *c.Name, err)
			}
			operatedControlPlaneInstance = (*controlPlaneInstance)[0]
			return nil
		},
		Name: "control plane instance",
		Replace: func(name string) error {
			controlPlaneInstance, err := controlPlaneInstanceValues.Replace(apiClient, apiEndpoint, name)
			if err != nil {
				return fmt.Errorf("failed to replace control plane definition with name %s: %w", name, err)
			}
			operatedControlPlaneInstance = *controlPlaneInstance
			return nil
		},
	})

	return &operations, &operatedControlPlaneDefinition, &operatedControlPlaneInstance
}
