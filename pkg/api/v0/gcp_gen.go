// boilerplate generated by 'threeport-sdk gen' - do not edit

package v0

import (
	"encoding/json"
	"fmt"
	notifications "github.com/threeport/threeport/pkg/notifications/v0"
	"time"
)

const (
	ObjectTypeGcpGkeKubernetesRuntimeDefinition string = "GcpGkeKubernetesRuntimeDefinition"
	ObjectTypeGcpGkeKubernetesRuntimeInstance   string = "GcpGkeKubernetesRuntimeInstance"
	ObjectTypeGcpProvider                       string = "GcpProvider"

	PathGcpGkeKubernetesRuntimeDefinitionVersions = "/gcp-gke-kubernetes-runtime-definitions/versions"
	PathGcpGkeKubernetesRuntimeDefinitions        = "/v0/gcp-gke-kubernetes-runtime-definitions"
	PathGcpGkeKubernetesRuntimeInstanceVersions   = "/gcp-gke-kubernetes-runtime-instances/versions"
	PathGcpGkeKubernetesRuntimeInstances          = "/v0/gcp-gke-kubernetes-runtime-instances"
	PathGcpProviderVersions                       = "/gcp-providers/versions"
	PathGcpProviders                              = "/v0/gcp-providers"
)

// NotificationPayload returns the notification payload that is delivered to the
// controller when a change is made.  It includes the object as presented by the
// client when the change was made.
func (ggkrd *GcpGkeKubernetesRuntimeDefinition) NotificationPayload(
	operation notifications.NotificationOperation,
	requeue bool,
	creationTime int64,
) (*[]byte, error) {
	notif := notifications.Notification{
		CreationTime:  &creationTime,
		Object:        ggkrd,
		ObjectVersion: ggkrd.GetVersion(),
		Operation:     operation,
	}

	payload, err := json.Marshal(notif)
	if err != nil {
		return &payload, fmt.Errorf("failed to marshal notification payload %+v: %w", ggkrd, err)
	}

	return &payload, nil
}

// DecodeNotifObject takes the threeport object in the form of a
// map[string]interface and returns the typed object by marshalling into JSON
// and then unmarshalling into the typed object.  We are not using the
// mapstructure library here as that requires custom decode hooks to manage
// fields with non-native go types.
func (ggkrd *GcpGkeKubernetesRuntimeDefinition) DecodeNotifObject(object interface{}) error {
	jsonObject, err := json.Marshal(object)
	if err != nil {
		return fmt.Errorf("failed to marshal object map from consumed notification message: %w", err)
	}
	if err := json.Unmarshal(jsonObject, &ggkrd); err != nil {
		return fmt.Errorf("failed to unmarshal json object to typed object: %w", err)
	}
	return nil
}

// GetId returns the unique ID for the object.
func (ggkrd *GcpGkeKubernetesRuntimeDefinition) GetId() uint {
	return *ggkrd.ID
}

// Type returns the object type.
func (ggkrd *GcpGkeKubernetesRuntimeDefinition) GetType() string {
	return "GcpGkeKubernetesRuntimeDefinition"
}

// Version returns the version of the API object.
func (ggkrd *GcpGkeKubernetesRuntimeDefinition) GetVersion() string {
	return "v0"
}

// NotificationPayload returns the notification payload that is delivered to the
// controller when a change is made.  It includes the object as presented by the
// client when the change was made.
func (ggkri *GcpGkeKubernetesRuntimeInstance) NotificationPayload(
	operation notifications.NotificationOperation,
	requeue bool,
	creationTime int64,
) (*[]byte, error) {
	notif := notifications.Notification{
		CreationTime:  &creationTime,
		Object:        ggkri,
		ObjectVersion: ggkri.GetVersion(),
		Operation:     operation,
	}

	payload, err := json.Marshal(notif)
	if err != nil {
		return &payload, fmt.Errorf("failed to marshal notification payload %+v: %w", ggkri, err)
	}

	return &payload, nil
}

// DecodeNotifObject takes the threeport object in the form of a
// map[string]interface and returns the typed object by marshalling into JSON
// and then unmarshalling into the typed object.  We are not using the
// mapstructure library here as that requires custom decode hooks to manage
// fields with non-native go types.
func (ggkri *GcpGkeKubernetesRuntimeInstance) DecodeNotifObject(object interface{}) error {
	jsonObject, err := json.Marshal(object)
	if err != nil {
		return fmt.Errorf("failed to marshal object map from consumed notification message: %w", err)
	}
	if err := json.Unmarshal(jsonObject, &ggkri); err != nil {
		return fmt.Errorf("failed to unmarshal json object to typed object: %w", err)
	}
	return nil
}

// GetId returns the unique ID for the object.
func (ggkri *GcpGkeKubernetesRuntimeInstance) GetId() uint {
	return *ggkri.ID
}

// Type returns the object type.
func (ggkri *GcpGkeKubernetesRuntimeInstance) GetType() string {
	return "GcpGkeKubernetesRuntimeInstance"
}

// Version returns the version of the API object.
func (ggkri *GcpGkeKubernetesRuntimeInstance) GetVersion() string {
	return "v0"
}

// ScheduledForDeletion returns a pointer to the DeletionScheduled timestamp
// if scheduled for deletion or nil if not scheduled for deletion.
func (ggkri *GcpGkeKubernetesRuntimeInstance) ScheduledForDeletion() *time.Time {
	return ggkri.DeletionScheduled
}

// NotificationPayload returns the notification payload that is delivered to the
// controller when a change is made.  It includes the object as presented by the
// client when the change was made.
func (gp *GcpProvider) NotificationPayload(
	operation notifications.NotificationOperation,
	requeue bool,
	creationTime int64,
) (*[]byte, error) {
	notif := notifications.Notification{
		CreationTime:  &creationTime,
		Object:        gp,
		ObjectVersion: gp.GetVersion(),
		Operation:     operation,
	}

	payload, err := json.Marshal(notif)
	if err != nil {
		return &payload, fmt.Errorf("failed to marshal notification payload %+v: %w", gp, err)
	}

	return &payload, nil
}

// DecodeNotifObject takes the threeport object in the form of a
// map[string]interface and returns the typed object by marshalling into JSON
// and then unmarshalling into the typed object.  We are not using the
// mapstructure library here as that requires custom decode hooks to manage
// fields with non-native go types.
func (gp *GcpProvider) DecodeNotifObject(object interface{}) error {
	jsonObject, err := json.Marshal(object)
	if err != nil {
		return fmt.Errorf("failed to marshal object map from consumed notification message: %w", err)
	}
	if err := json.Unmarshal(jsonObject, &gp); err != nil {
		return fmt.Errorf("failed to unmarshal json object to typed object: %w", err)
	}
	return nil
}

// GetId returns the unique ID for the object.
func (gp *GcpProvider) GetId() uint {
	return *gp.ID
}

// Type returns the object type.
func (gp *GcpProvider) GetType() string {
	return "GcpProvider"
}

// Version returns the version of the API object.
func (gp *GcpProvider) GetVersion() string {
	return "v0"
}
